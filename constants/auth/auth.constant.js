const ACCESS_TOKEN_TTL = 15;
const REFRESH_TOKEN_TTL = 30;
const PENDING_TOKEN_TTL = 1;
const PASSWORD_RESET_SESSION_TTL_MINUTES = 10;

const ACCESS_TOKEN_EXPIRES_IN = `${ACCESS_TOKEN_TTL}m`;
const REFRESH_TOKEN_EXPIRES_IN = `${REFRESH_TOKEN_TTL}d`;
const PENDING_TOKEN_EXPIRES_IN = `${PENDING_TOKEN_TTL}h`;

const ACCESS_TOKEN_EXPIRES_IN_MS = ACCESS_TOKEN_TTL * 60 * 1000;
const REFRESH_TOKEN_EXPIRES_IN_MS = REFRESH_TOKEN_TTL * 24 * 60 * 60 * 1000;
const PENDING_TOKEN_EXPIRES_IN_MS = PENDING_TOKEN_TTL * 60 * 60 * 1000;
const PASSWORD_RESET_SESSION_EXPIRES_IN_MS =
  PASSWORD_RESET_SESSION_TTL_MINUTES * 60 * 1000;

const COOL_DOWN_PERIODS_IN_SECONDS = {
  EMAIL_VERIFICATION: 300,
  PASSWORD_RESET: 300,
};

const VERIFICATION_TYPES = {
  EMAIL_ACTIVATE: "email_activate",
  PASSWORD_RESET: "password_reset",
};

module.exports = {
  ACCESS_TOKEN_EXPIRES_IN,
  REFRESH_TOKEN_EXPIRES_IN,
  PENDING_TOKEN_EXPIRES_IN,
  PASSWORD_RESET_SESSION_TTL_MINUTES,

  ACCESS_TOKEN_EXPIRES_IN_MS,
  REFRESH_TOKEN_EXPIRES_IN_MS,
  PENDING_TOKEN_EXPIRES_IN_MS,
  PASSWORD_RESET_SESSION_EXPIRES_IN_MS,

  COOL_DOWN_PERIODS_IN_SECONDS,
  VERIFICATION_TYPES,
};
